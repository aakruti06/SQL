-- create table
CREATE TABLE st (
	id INT AUTO_INCREMENT PRIMARY KEY,
	name VARCHAR(30),
	marks INT
);

-- insert data
INSERT INTO st (name, marks) VALUES
('FIG' 80),
('PEAR', 90);

SELECT * FROM st;

